To setup Kafka with mTLS, we must use script `create-kafka-cluster-ssl.sh` to help to generate correct SSL, in the future we will use KMS or some similar like that from Vault to setup this one instead

