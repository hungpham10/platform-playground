---
# SSH user for all nodes
ssh_user: "{{ ansible_user }}"
ssh_home: "/home/{{ ssh_user }}"

# SSH key name (will be created in ~/.ssh/)
ssh_key_name: "id_ed25519"

# SSH key type and size
ssh_key_type: "ed25519"
ssh_key_size: 4096
