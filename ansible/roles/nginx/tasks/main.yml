- name: Create Nginx directories
  file:
    path: "{{ item }}"
    state: directory
  loop:
    - "{{ nginx_ssl_dir }}"

- name: Render nginx.conf
  template:
    src: nginx.conf.j2
    dest: "{{ nginx_home }}/nginx.conf"
  notify: Restart nginx
