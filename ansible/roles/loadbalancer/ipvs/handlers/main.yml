---
- name: reload systemd
  systemd:
    daemon_reload: yes

- name: restart keepalived
  systemd:
    name: keepalived
    state: restarted

- name: reload ipvs services
  command: "{{ ipvs_config_dir }}/ipvs-services.sh"