instances:
{% for host in groups['elasticsearch-nodes'] %}
  - name: "{{ host }}"
    dns:
      - "{{ hostvars[host]['full_hostname'] | default(host) }}"
      - "{{ host }}"
      - "localhost"
    ip:
      - "{{ hostvars[host]['ansible_host'] }}"
{% endfor %}
