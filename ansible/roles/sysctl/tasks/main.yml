- name: Set additional sysctl parameters for DNS performance
  ansible.builtin.sysctl:
    name: "{{ item.name }}"
    value: "{{ item.value }}"
    state: present
    sysctl_file: /etc/sysctl.conf
    reload: yes
  loop: "{{ sysctl_configs | default([]) }}"
