---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: storage-test
    image: ubuntu:22.04
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    command: /sbin/init
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible